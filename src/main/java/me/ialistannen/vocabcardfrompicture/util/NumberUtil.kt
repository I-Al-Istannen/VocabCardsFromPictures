package me.ialistannen.vocabcardfrompicture.util

fun divideInRanges(numbers: List<Double>, rangeSize: Double): List<Double> {
    if (numbers.isEmpty()) {
        return emptyList()
    }
    val min = numbers.min()!!
    val max = numbers.max()!!

    TODO("Continue here...")
    return emptyList()
}